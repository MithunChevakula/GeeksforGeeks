import statistics
class Solution:
    def median(self, mat):
        new = []
        for row in matrix:
            for element in row:
               new.append(element)
        return statistics.median(new)    